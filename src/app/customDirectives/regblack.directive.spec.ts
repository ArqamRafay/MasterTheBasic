import { RegblackDirective } from './regblack.directive';

describe('RegblackDirective', () => {
  it('should create an instance', () => {
    const directive = new RegblackDirective();
    expect(directive).toBeTruthy();
  });
});
